var Snake = Snake || {};

(function(app){

    // merge in options
    // app.mixin.enable = function(options) {
    //   for (var key in options) {
    //     this[key] = options[key];
    //   }
    // };

})(Snake);